// Global configuration object
const CONFIG = {
  // Server base URL - can be easily changed for different environments
  SERVER_URL: 'http://192.168.1.140:8080/commerce/',
  
  // Add other global configuration settings as needed
};
